{
  "name": "mono",
  "scripts": {
    "dev": "update-ts-references && tsc -b --extendedDiagnostics",
    "build:bunup": "bunup && update-ts-references && tsc --extendedDiagnostics",
    "build:vite": " && update-ts-references && tsc --extendedDiagnostics",
    "postinstall": "update-ts-references"
  },
  "engines": {
    "node": "22.16.0",
    "bun": ">=1.2.15 <2"
  },
  "workspaces": {
    "packages": [
      "sources/*",
      "outputs/*"
    ],
    "catalog": {
      "typescript": "^5.8.3",
      "bunup": "^5.8.3",
      "@types/bun": "^1.2.15"
    }
  },
  "devDependencies": {
    "vite": "npm:rolldown-vite@latest",
    "update-ts-references": "^3.6.0"
  },
  "overrides": {
    "vite": "npm:rolldown-vite@latest"
  },
  "version": "1.0.0",
  "description": ""
}